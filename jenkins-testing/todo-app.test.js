// todo-app.test.js

const { expect } = require('expect'); // Import Jest's assertion library

// Function you want to test (replace with your actual function)
function addNumbers(a, b) {
  return a + b;
}

// Test case (replace with your actual test logic)
test('adds 1 + 2 to equal 3', () => {
  expect(addNumbers(1, 2)).toBe(3);
});
